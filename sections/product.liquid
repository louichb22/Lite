<div style="background-color:{{ section.settings.bg }}">
  <div class="section_container_1200">
    <div class="grid" style="grid-template-columns:50% 50%; align-items:start">
      {% for block in section.blocks %}
      {% case block.type %}
      {% when "image" %}
      <div class="product_image_container" style="border-color:{{ section.settings.border_color }};">
        <img class="product_image" src="{{ product.featured_image | image_url }}">
        <div class="product_image_carrousel">
        {% for image in product.images %}
          <div class="product_image_thumbnail" style="background-image: url('{{ image | image_url }}')"></div>
        {% endfor %}
        </div>
      </div>
      {% when "text" %}
      <div class="product_info" style="border-color:{{ section.settings.border_color }};">
        <h1 class="product_title" style="color:{{ block.settings.title_color }}">{{ product.title }}</h1>
        <h6 class="product_vendor" style="color:{{ block.settings.vendor_color }}">{{ product.vendor }}</h6>
      </div>
      {% endcase %}{% endfor %}
    </div>
  </div>
</div>

{% schema %}
{
  "name": "Product",
  "settings": [
    {
      "type": "color",
      "id": "bg",
      "label": "Couleur de fond"
    },
    {
      "type": "color",
      "id": "border_color",
      "label": "Couleur de la bordure"
    }
  ],
  "blocks": [
    {
      "name": "Image",
      "type": "image",
      "limit": 1,
      "settings": [
        {
          "type": "header",
          "content": "Images de produit"
        }
      ]
    },
    {
      "name": "Texte",
      "type": "text",
      "limit": 1,
      "settings": [
        {
          "type": "header",
          "content": "Couleurs"
        },
        {
          "type": "color",
          "id": "title_color",
          "label": "Nom du produit"
        },
        {
          "type": "color",
          "id": "vendor_color",
          "label": "Vendeur"
        }
      ]
    }
  ],
  "default": {
    "settings": {
      "bg": "#FAFAFA"
    },
    "blocks": [
      {
        "type": "image"
      },
      {
        "type": "text"
      }
    ]
  }
}
{% endschema %}
